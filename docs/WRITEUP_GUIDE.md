# 롸잇업 작성 가이드

이 프로젝트의 모든 신규 롸잇업은 이 문서의 형식을 따른다.

좋은 롸잇업은 성공한 명령어만 모아 둔 기록이 아니다. 처음 무엇을 관찰했고, 그 사실에서 왜 특정 가설을 세웠으며, 어떤 명령어로 검증했고, 결과를 어떻게 해석해 다음 단계로 넘어갔는지가 독자에게 자연스럽게 이어져야 한다.

기준 예시는 [`completed/red/dreamhack_path_traversal.md`](completed/red/dreamhack_path_traversal.md)다.

---

## 필수 원칙

- 사고 흐름을 생략하지 않는다. `관찰 → 판단 → 가설 → 검증 → 결과 해석 → 다음 행동`의 연결을 적는다.
- 사용한 명령어는 복사해서 다시 실행할 수 있는 완전한 형태로 기록한다.
- 명령어와 함께 주요 옵션을 왜 사용했는지 설명한다.
- 단순히 "취약해 보였다"고 쓰지 않고, 어떤 응답·코드·동작을 근거로 판단했는지 적는다.
- 확인된 사실과 추정을 구분한다. 추정 코드나 예상 경로에는 추정임을 명시한다.
- 실패한 시도도 다음 판단에 영향을 주었다면 목적, 결과, 배운 점을 짧게 남긴다.
- 성공 페이로드만 제시하지 않고 정상 동작을 먼저 확인하여 비교 기준을 만든다.
- 마지막에는 최소 재현 명령어, 근본 원인, 안전한 대응 방향을 포함한다.
- 공개 문서의 플래그, 토큰, 비밀번호 등 민감한 값은 `REDACTED`로 마스킹한다.

---

## 필수 문서 구조

문제 특성에 따라 세부 제목은 바꿀 수 있지만, 아래 내용은 빠뜨리지 않는다.

### 1. 제목

플랫폼, 문제명, 핵심 취약점 또는 공격 기법이 드러나게 작성한다.

```markdown
# DREAMHACK CTF - Path Traversal
```

### 2. Challenge Info

대상, 분야, 취약점, 난이도 등 문제의 기본 정보를 짧게 정리한다.

```markdown
## Challenge Info

- Target: `https://target.example/`
- Category: Web
- Vulnerability: Path Traversal
- Difficulty: Easy
```

### 3. Goal

무엇을 확인하거나 획득해야 하는지, 문제의 핵심 입력과 기능이 무엇인지 설명한다.

### 4. 사고 흐름

전체 풀이를 번호 순서로 먼저 요약한다. 각 항목에는 행동뿐 아니라 행동한 이유가 드러나야 한다.

```markdown
## 사고 흐름

1. 루트 페이지에서 노출된 엔드포인트를 확인했다.
2. 입력값을 바꾸는 JavaScript가 클라이언트에서만 실행되므로 직접 요청으로 우회할 수 있다고 판단했다.
3. 정상 입력 두 개의 응답 차이를 비교해 서버가 입력값에 대응하는 파일을 읽는다고 가설을 세웠다.
4. `../` 페이로드로 가설을 검증했다.
```

`확인했다`, `판단했다`, `추정했다`, `검증했다`를 구분해 쓰면 사실과 추론이 명확해진다.

### 5. Surface Recon

최초 접근부터 공격 표면을 좁힌 과정을 기록한다.

- 확인한 URL, 포트, 폼, 파라미터, 헤더, 소스 코드
- 실제 실행한 명령어와 필요한 응답 일부
- 해당 결과에서 중요하게 본 단서
- 그 단서가 다음 행동으로 이어진 이유

응답 전문을 무작정 붙이지 말고 판단에 필요한 부분만 인용한다.

### 6. Baseline Behavior

공격 페이로드를 넣기 전에 정상 입력과 정상 응답을 확인한다.

- 정상 요청은 어떤 형태인지
- 입력에 따라 응답이 어떻게 달라지는지
- 비교 결과로 무엇을 확인했고 무엇을 아직 단정할 수 없는지

정상 기준이 있어야 공격 결과의 차이를 설명할 수 있다.

### 7. 사용한 명령어와 옵션 설명

명령어는 코드 블록으로 적고, 주요 옵션의 역할과 선택 이유를 설명한다.

```bash
curl -sS -i --max-time 10 "https://target.example/"
```

- `-sS`: 진행률은 숨기되 오류는 확인하기 위해 사용했다.
- `-i`: 상태 코드와 응답 헤더를 함께 보기 위해 사용했다.
- `--max-time 10`: 응답이 없을 때 반복 검증이 멈추지 않도록 제한했다.

모든 셸 문법을 사전처럼 설명할 필요는 없다. 결과 재현과 판단에 중요한 옵션, 파이프라인, 인코딩 방식, 필터링 조건을 설명한다. 같은 옵션이 반복되면 최초 등장 위치에서 자세히 설명하고 이후에는 링크하거나 짧게 언급한다.

### 8. 가설 수립

관찰한 사실로부터 세운 가설을 명시한다.

```text
관찰: 입력값마다 서로 다른 사용자 정보가 반환됐다.
판단: 서버가 입력값을 키 또는 파일명으로 사용하고 있을 가능성이 있다.
가설: 입력값이 파일 경로에 직접 결합된다면 `../`로 상위 경로에 접근할 수 있다.
검증: 디렉터리 순회 문자열을 인코딩해 직접 전송한다.
```

가설을 사실처럼 쓰지 않는다. 서버 소스가 없으면 구현 예시는 반드시 "동작을 바탕으로 한 추정"이라고 밝힌다.

### 9. Exploitation 또는 검증 과정

다음 내용을 순서대로 기록한다.

1. 페이로드를 선택한 이유
2. 실행한 완전한 명령어
3. 핵심 응답 또는 결과
4. 성공·실패 판단 기준
5. 결과가 가설을 어떻게 지지하거나 반박하는지

여러 후보를 반복 시험했다면 반복문의 범위와 각 후보를 고른 이유도 설명한다. 실패 결과가 경로, 확장자, 필터 존재 여부를 좁히는 데 도움을 줬다면 함께 기록한다.

### 10. Root Cause

증상이 아니라 취약점이 발생한 구조적 원인을 설명한다.

- 신뢰할 수 없는 입력이 어디로 전달됐는지
- 어떤 검증 또는 권한 통제가 빠졌는지
- 클라이언트와 서버 중 어디에서 검증해야 하는지
- 추정 코드라면 실제 코드가 아님을 명시
- 가능하면 더 안전한 구현 방향과 짧은 예시

### 11. Key Takeaways

다른 문제나 실무에도 적용할 수 있는 교훈을 3~5개로 정리한다. 앞 내용을 그대로 반복하기보다 재사용 가능한 판단 기준을 남긴다.

### 12. Reproducible Final Command

풀이를 가장 짧게 재현하는 최종 명령어를 제공한다. 필요한 URL, 메서드, 파라미터와 옵션이 모두 포함되어야 한다.

### 13. Final Flag 또는 최종 결과

최종 결과를 명확하게 표시하되 공개 저장소에서는 민감한 원문을 마스킹한다.

```markdown
## Final Flag

`DH{REDACTED}`
```

---

## 문장 작성 기준

아래처럼 행동과 근거를 한 흐름으로 연결한다.

좋은 예:

> 폼 제출 직전에 JavaScript가 `guest`를 `0`으로 바꾸는 것을 확인했다. 이 변환은 브라우저에서만 수행되므로 서버의 입력 검증으로 볼 수 없다. 따라서 `curl`로 `userid` 값을 직접 전송해 임의 입력이 처리되는지 검증했다.

피해야 할 예:

> JS를 보고 curl을 썼다. `../flag`를 넣으니 풀렸다.

명령어 뒤에는 결과를 나열하는 데서 끝내지 말고 반드시 해석을 붙인다.

```markdown
응답이 `200 OK`였다는 사실만으로 파일 읽기를 단정할 수는 없다. 다만 정상 사용자마다 다른 JSON이 반환됐고 문제 제목이 Path Traversal이므로, 파일 기반 조회 가능성을 다음 단계에서 검증할 근거는 충분했다.
```

---

## 제출 전 체크리스트

- [ ] 제목과 Challenge Info가 있는가?
- [ ] 목표와 전체 사고 흐름이 먼저 요약되어 있는가?
- [ ] 관찰한 사실과 추정한 내용을 구분했는가?
- [ ] 각 판단에 근거가 적혀 있는가?
- [ ] 정상 동작을 먼저 확인했는가?
- [ ] 명령어가 그대로 재현 가능한가?
- [ ] 주요 옵션과 페이로드를 왜 사용했는지 설명했는가?
- [ ] 응답 결과를 해석하고 다음 단계와 연결했는가?
- [ ] 실패한 시도 중 의미 있는 내용이 기록되어 있는가?
- [ ] Root Cause와 안전한 대응 방향이 있는가?
- [ ] Key Takeaways와 최종 재현 명령어가 있는가?
- [ ] 플래그와 민감 정보가 마스킹되어 있는가?

체크리스트를 충족하지 않은 롸잇업은 완료본으로 취급하지 않는다.
